version: 1.0.1

plugins:
  - jekyll-paginate

# Dependencies
highlighter:  rouge
markdown:	  kramdown

# Permalinks
permalink:        pretty

# Setup
url:              "https://www.szylhabel.pl"
baseurl:          ""

paginate:         5

exclude: ["node_modules",
  "gulpfile.js",
  "package.json",
  "Gemfile",
  "Gemfile.lock",
  "package-lock.json",
  ".travis.yml",
  "README.md"]
