plugins:
  - jekyll-paginate

# Dependencies
highlighter:  rouge
markdown:	  kramdown

# Permalinks
permalink:        pretty

# Setup
baseurl:          /

paginate:         5

exclude: ["node_modules",
  "gulpfile.js",
  "package.json",
  "Gemfile",
  "Gemfile.lock",
  "package-lock.json",
  ".travis.yml",
  "README.md"]
